##############################################################################
# Copy to sa-config.local.yaml for Windows dev (gitignored).
# Merged on top of sa-config.yaml — only dev-only overrides belong here.
##############################################################################

# LAN IP of the Pi running SolarAssistant (sa-config.yaml uses localhost on Pi).
sa:
  host: "192.168.1.100"
