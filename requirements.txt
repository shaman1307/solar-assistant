fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
pyyaml>=6.0.1
requests>=2.31.0
apscheduler>=3.10.4
# py-solar-assistant requires Python >=3.12 per PyPI metadata, but the wheel
# is pure Python and works on 3.11.  Install with --ignore-requires-python.
py-solar-assistant>=0.2.1
